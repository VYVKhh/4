# تعليمات النشر على Fly.io

هذه ثلاثة ملفات جاهزة: `4.js` (الخادم)، `Dockerfile`، و`fly.toml`.
ضعها الثلاثة في نفس المجلد على جهازك، ثم نفّذ الخطوات التالية بالترتيب.

## 1) التثبيت وتسجيل الدخول (مرة واحدة فقط)
```
# ثبّت flyctl (macOS/Linux)
curl -L https://fly.io/install.sh | sh

# سجّل الدخول (سيفتح المتصفح لإنشاء/تسجيل حساب)
fly auth login
```
على ويندوز استخدم الأمر الموجود في صفحة التثبيت الرسمية:
https://fly.io/docs/flyctl/install/

## 2) افتح مجلد المشروع
```
cd المسار/إلى/مجلد/المشروع
```
تأكد أن الملفات الثلاثة (`4.js`, `Dockerfile`, `fly.toml`) موجودة فيه.

## 3) غيّر اسم التطبيق
افتح `fly.toml` وغيّر السطر:
```
app = "multaqa-ilm-thaqafi"
```
إلى اسم فريد من اختيارك (إنجليزي، بدون مسافات)، لأن هذا الاسم يصبح جزءًا
من رابط موقعك: `https://اسمك.fly.dev`

## 4) أنشئ القرص الدائم (لحفظ المحتوى والصور بشكل دائم)
```
fly volumes create site_data --size 1 --region fra
```
غيّر `fra` لو اخترت منطقة مختلفة في `fly.toml`.

## 5) اضبط اسم المستخدم وكلمة المرور بأمان
```
fly secrets set ADMIN_USERNAME=اسمك ADMIN_PASSWORD='كلمة_مرور_قوية'
```
(استبدل القيمتين بما تريد فعليًا — لا تترك المثال كما هو)

## 6) انشر
```
fly deploy
```
سيستغرق دقيقة أو دقيقتين. في النهاية سيعطيك رابط موقعك جاهزًا.

## بعد النشر
- الموقع: `https://اسمك.fly.dev`
- لوحة الإدارة: `https://اسمك.fly.dev/admin`
- لأي تحديث لاحق للكود: عدّل `4.js` ثم نفّذ `fly deploy` فقط.

## نسخة احتياطية دورية (مهم)
بين فترة وأخرى نزّل نسخة من مجلد data من الخادم:
```
fly ssh sftp get -a اسمك /app/data ./نسخة-احتياطية-data
```
هذا يحفظ المحتوى والصور وبيانات الحساب على جهازك احتياطًا.
